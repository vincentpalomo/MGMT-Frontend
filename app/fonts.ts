import { Racing_Sans_One, Roboto_Mono } from 'next/font/google';

export const racing_sans_one = Racing_Sans_One({
  weight: '400',
  subsets: ['latin'],
});

export const roboto_mono = Roboto_Mono({
  subsets: ['latin'],
  display: 'swap',
});
